const initialState = {
    loading: true
};

const microcmsReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default microcmsReducer;